# Copy to web/.env (or use ../.env with SUPABASE_* names — both work locally)
VITE_SUPABASE_URL=https://crulqsufbijcfdskggkz.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_publishable_key_here

# Vercel: use the VITE_* names above (required at build time), then Redeploy

# Auth users (created via scripts/setup_auth_users.py):
# Admin: admin@cragleigh.local / 123admin
# View only: user@cragleigh.local / readonly
